{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core invoke/event access plus opener, updater, and notification.",
  "windows": ["main", "banner"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default",
    "notification:default"
  ]
}
